# Checkpoint 86 - README Onboarding

## What changed

- Rewrote the README around the actual user path: install, first run, setup, update, and source checkout.
- Put the macOS/Linux one-line terminal installer near the top.
- Put the GitHub-to-PC update command near the top: `aegis update --approved`.
- Added the progressive setup path with `aegis setup next` and `/setup next`.
- Kept optional web usage clearly separated from normal terminal install and use.

## Verification

Planned before commit:

- README command-copy inspection.
- Markdown diff check.
- Installed command smoke for `aegis setup next`, `aegis activation`, and `aegis update` preview.
